/* =========================================================
   Suporte Informática — config.example.js
   MODELO de configuração (este arquivo É versionado no Git).

   Como usar:
   1. Copie este arquivo para "config.js"
   2. Substitua o valor de web3formsKey pela sua chave real
      (obtida em https://web3forms.com)
   3. NÃO commite o config.js — ele já está no .gitignore

   A chave do Web3Forms é uma access key pública (enviada pelo
   navegador do visitante). Para proteção real, ative a
   "Domain Restriction" no painel do Web3Forms, limitando a
   chave ao seu domínio (ex.: suporterio.com.br).
   ========================================================= */
window.APP_CONFIG = {
  // Substitua pela sua chave real do Web3Forms
  web3formsKey: "SUA_CHAVE_WEB3FORMS_AQUI",
};
